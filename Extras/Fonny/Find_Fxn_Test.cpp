#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;

int main()
{
    vector <int> random;
    random.push_back(10);
    random.push_back(12);
    random.push_back(40);
    random.push_back(15);

    int z= 10;
    it = find(random.begin(),random.end(),int z);

    cout<<f<<endl;
    return 0;
}